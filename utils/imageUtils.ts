/**
 * Arquivo de utilitários de imagem.
 * A lógica de compressão foi removida temporariamente para simplificar o projeto.
 */

export const compressImage = async (file: File): Promise<File> => {
  // Retorna o arquivo original sem alterações por enquanto
  return file;
};
